const labels: Record<string, string> = { VALID: "Ready", ESCALATION: "Needs your review", REJECTED: "Excluded", PARTIAL_FAILURE: "Some records failed", RETRYABLE_FAILURE: "Temporary failure", PERMANENT_FAILURE: "Needs correction", ROLLED_BACK: "Undone", NEEDS_REVIEW: "Needs your review", COMPLETED: "Complete", SUCCESS: "Sent", QUEUED: "Waiting to start", RUNNING: "Working", PAUSED: "Waiting for your review", ACTIVE: "In progress", CANCELLED: "Stopped", COMPLETED_WITHOUT_PUSH: "Finished without sending" };
export function StatusPill({ value }: { value: string }) { return <span className={`status status-${value.toLowerCase().replaceAll("_", "-")}`}>{labels[value] ?? value.replaceAll("_", " ").toLowerCase()}</span>; }
